# Backend API (unused by the landing page itself — kept for local tooling)
VITE_API_URL=http://localhost:3001
VITE_ADMIN_PASSWORD=

# Supabase — waitlist capture + the signup counter.
# Safe to expose: anon may only INSERT into waitlist_signups and EXECUTE
# waitlist_count(). See client/supabase/waitlist.sql.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY
